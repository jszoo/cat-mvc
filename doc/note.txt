NOTE
1. Simply change every dependency's version to *, then run npm update --save.
2. npm-check-updates is a utility that automatically adjusts a package.json with the latest version of all dependencies
$ npm install -g npm-check-updates
$ npm-check-updates -u
$ npm install 